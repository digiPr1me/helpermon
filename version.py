"""The one place the version number is written down.

It is what the About page shows and what a feedback report carries, so it
matches the release tag on GitHub -- v1.1 -- rather than counting up on its
own: a report that names a version has to name one somebody can look up.
"""

VERSION = "1.1"
